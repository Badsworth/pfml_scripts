[
  {
    "name": "${task_name}",
    "image": "${docker_image}",
    "command": ${command},
    "cpu": ${cpu},
    "memory": ${memory},
    "networkMode": "awsvpc",
    "essential": true,
    "readonlyRootFilesystem": false,
    "linuxParameters": {
      "capabilities": {
        "drop": ["ALL"]
      },
      "initProcessEnabled": true
    },
    "logConfiguration": {
      "logDriver": "awslogs",
      "options": {
        "awslogs-group": "${cloudwatch_logs_group_name}",
        "awslogs-region": "${aws_region}",
        "awslogs-stream-prefix": "${environment_name}"
      }
    },
    "environment": [
      { "name": "ENVIRONMENT", "value": "${environment_name}" },
      { "name": "DB_HOST", "value": "${db_host}" },
      { "name": "DB_NAME", "value": "${db_name}" },
      { "name": "DB_USERNAME", "value": "${db_username}" },
      { "name": "LOGGING_LEVEL", "value": "${logging_level}" },

      { "name": "S3_BUCKET", "value": "s3://massgov-pfml-${environment_name}-agency-transfer/" },
      { "name": "S3_DFML_ARCHIVE_DIRECTORY_PATH", "value": "reductions/dfml/archive" },
      { "name": "S3_DFML_OUTBOUND_DIRECTORY_PATH", "value": "reductions/dfml/outbound" },
      { "name": "S3_DFML_PENDING_DIRECTORY_PATH", "value": "reductions/dfml/pending" }
    ],
    "secrets": [
      { "name": "DB_PASSWORD", "valueFrom": "/service/${app_name}/${environment_name}/db-password" }
    ]
  }
]
