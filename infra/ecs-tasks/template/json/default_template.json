[
  {
    "name": "${task_name}",
    "image": "${docker_image}",
    "command": ${command},
    "cpu": ${cpu},
    "memory": ${memory},
    "networkMode": "awsvpc",
    "essential": true,
    "readonlyRootFilesystem": true,
    "linuxParameters": {
      "capabilities": {
        "drop": ["ALL"]
      },
      "initProcessEnabled": true
    },
    "logConfiguration": {
      "logDriver": "awslogs",
      "options": {
        "awslogs-group": "${cloudwatch_logs_group_name}",
        "awslogs-region": "${aws_region}",
        "awslogs-stream-prefix": "${environment_name}"
      }
    },
    "environment": [
      { "name": "ENVIRONMENT", "value": "${environment_name}" },
      { "name": "DB_HOST", "value": "${db_host}" },
      { "name": "DB_NAME", "value": "${db_name}" },
      { "name": "DB_USERNAME", "value": "${db_username}" },
      { "name": "ENABLE_SENTRY", "value": "${enable_sentry}"}
    ],
    "secrets": [
      { "name": "DB_PASSWORD", "valueFrom": "/service/${app_name}/${environment_name}/db-password"},
      { "name": "COGNITO_FINEOS_APP_CLIENT_ID", "valueFrom": "/service/${app_name}/${environment_name}/cognito_fineos_app_client_id"},
      { "name": "COGNITO_INTERNAL_FINEOS_ROLE_APP_CLIENT_ID", "valueFrom": "/service/${app_name}/${environment_name}/cognito_internal_fineos_role_app_client_id"}
    ]
  }
]
