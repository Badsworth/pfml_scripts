import React from "react";
import RecordNotFound from "./RecordNotFound";

export default {
  title: "Eligibility Result|RecordNotFound",
  component: RecordNotFound,
};

export const Default = () => <RecordNotFound />;
