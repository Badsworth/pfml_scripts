# Run tests, linting, and storybook for the Paid Leave website as part of continuous integration.
#
  name: Portal Website Testing

  on:
    pull_request:
      paths:
      - portal/**

  env:
    nodeVersion: 12.x

  jobs:
    lint:
      runs-on: ubuntu-latest

      steps:
      - uses: actions/checkout@v2

      - name: Use Node.js
        uses: actions/setup-node@v1
        with:
          node-version: ${{ env.nodeVersion }}

      - name: install
        run:
          npm ci

      - name: lint
        run:
          npm run lint

    test:
      runs-on: ubuntu-latest

      steps:
      - uses: actions/checkout@v2

      - name: Use Node.js
        uses: actions/setup-node@v1
        with:
          node-version: ${{ env.nodeVersion }}

      - name: install
        run:
          npm ci --prefix portal

      - name: test-cov
        run:
          npm test --prefix portal
    
    storybook:
      runs-on: ubuntu-latest

      steps:
      - uses: actions/checkout@v2

      - name: Use Node.js
        uses: actions/setup-node@v1
        with:
          node-version: ${{ env.nodeVersion }}

      - name: install
        run:
          npm ci --prefix portal

      - name: Smoke test Storybook
        run: npm run docs:smoke-test --prefix portal
